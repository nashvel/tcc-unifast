{
	frankenphp
	order php_server before file_server

	# Railway terminates TLS at its edge, so the container serves plain HTTP.
	auto_https off
	admin off
}

:{$PORT:8080} {
	root /app/public
	encode zstd br gzip

	php_server {
		index index.php
	}

	log {
		output stderr
		format json
	}
}
