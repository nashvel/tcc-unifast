TESSERACT_CMD=
TESSERACT_PSM=6
SAVE_DEBUG_IMAGES=false

# Shared secret required on /ocr/image and /ocr/pdf (header: X-OCR-Key).
# Must match OCR_API_KEY in the Laravel backend env.
# Blank = endpoints stay open and the service logs a warning (local dev only).
OCR_API_KEY=
