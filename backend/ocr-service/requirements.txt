fastapi
uvicorn
python-multipart
pytesseract
# Must be >=4.8.0 for cv2.FaceDetectorYN (YuNet) and cv2.FaceRecognizerSF (SFace) DNN APIs.
opencv-python-headless>=4.8.0
# For one-time ONNX model weight download at container startup.
requests>=2.31.0
Pillow
PyMuPDF
numpy
pydantic-settings
pytest
httpx
# School ID back QR + gradeslip-aligned decode. Windows: wheels include
# libzbar-64.dll; if import fails, install VC++ Redistributable and place
# libiconv.dll under ocr-service/native/ (or set ZBAR_DLL_PATH). Soft-fails OCR.
pyzbar>=0.1.9
# DOCX masterlist table extraction (CHED uploads)
python-docx>=1.1.2
